import { Timestamp } from 'rxjs';

export class Employee {
    id: string;
    firstName: string;
    lastName: string;
    age: number;
    email: string;
    sex: string;
    dateOfBirth: Date;
    carrerIds: any;
}